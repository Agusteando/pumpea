feat: refine 3D ecosystem graphic and rebuild aliados section

- Reworked the Three.js ecosystem graphic in the “Todo lo que tu negocio necesita” banner to better match the reference: translucent beveled blocks, layered top plate, soft blue glass, network nodes, floor lines, sparkles, and calm hover motion.
- Tuned the canvas scale and scene composition so the 3D graphic reads closer to the supplied isometric cube reference instead of a generic block cluster.
- Rebuilt the /aliados-clientes page to follow the provided reference layout with a trust hero, metric strip, client logo panel, testimonial panel, and CTA banner.
- Replaced generic partner-style claims with tools PUMPEA actually uses: Google Workspace, GitHub, and Vercel.
- Kept the implementation dependency-light with direct vanilla Three.js and excluded package-lock.json from the deliverable.
